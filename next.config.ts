import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
   images: {
    domains: ["images.unsplash.com", "cdn.pixabay.com"], // add your external domains here
  },
};

export default nextConfig;
